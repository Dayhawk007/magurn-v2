# Magurn

1. Setup

    - Run in bash

        ```sh
        python -m venv .venv && source .venv/Scripts/activate && pip install -r requirements.txt
        ```

2. Run

    ```sh
    python app.py
    ```